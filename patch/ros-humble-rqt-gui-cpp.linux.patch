diff --git a/CMakeLists.txt b/CMakeLists.txt
index df6a3ac..debc486 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -28,6 +28,22 @@ find_package(rclcpp REQUIRED)
 find_package(qt_gui REQUIRED)
 find_package(qt_gui_cpp REQUIRED)
 
+# A dumb change to remove the last item from Qt6Widgets include directories as it
+# points to the `_build_env/...` Gl. This folder doesn't stay constant across builds!
+foreach(ICDIR ${Qt6Widgets_INCLUDE_DIRS})
+  if (ICDIR MATCHES _build_env/x86_64-conda-linux-gnu/sysroot/usr/include OR
+      ICDIR MATCHES _build_env/aarch64-conda-linux-gnu/sysroot/usr/include)
+    message("REMOVING ${ICDIR}")
+    list(REMOVE_ITEM Qt6Widgets_INCLUDE_DIRS ${ICDIR})
+    break()
+  endif()
+endforeach()
+if(Qt6Widgets_INCLUDE_DIRS)
+  message(${Qt6Widgets_INCLUDE_DIRS})
+endif()
+# find_path(GL_PATH GL/gl.h)
+# list(REMOVE_ITEM Qt6Widgets_INCLUDE_DIRS GL_PATH)
+
 ament_export_dependencies(
   Qt6Widgets
   pluginlib
