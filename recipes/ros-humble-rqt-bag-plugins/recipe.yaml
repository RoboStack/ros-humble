package:
  name: ros-humble-rqt-bag-plugins
  version: 1.1.3
source:
  git_url: https://github.com/ros2-gbp/rqt_bag-release.git
  git_rev: release/humble/rqt_bag_plugins/1.1.3-2
  folder: ros-humble-rqt-bag-plugins/src/work

build:
  script:
    sel(win): bld_ament_python.bat
    sel(unix): build_ament_python.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.1 humble
    - setuptools
  run:
    - pillow
    - pycairo
    - python
    - ros-humble-geometry-msgs
    - ros-humble-rclpy
    - ros-humble-ros-workspace
    - ros-humble-rosbag2
    - ros-humble-rqt-bag
    - ros-humble-rqt-gui
    - ros-humble-rqt-gui-py
    - ros-humble-rqt-plot
    - ros-humble-sensor-msgs
    - ros-humble-std-msgs
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
