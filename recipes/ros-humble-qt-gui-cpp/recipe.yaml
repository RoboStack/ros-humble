package:
  name: ros-humble-qt-gui-cpp
  version: 2.2.1
source:
  git_url: https://github.com/ros2-gbp/qt_gui_core-release.git
  git_rev: release/humble/qt_gui_cpp/2.2.1-2
  folder: ros-humble-qt-gui-cpp/src/work
  patches:
    - patch/ros-humble-qt-gui-cpp.patch

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
    - sel(build_platform != target_platform): pkg-config
    - sel(build_platform != target_platform): pyqt-builder
    - sel(linux): "{{ cdt('mesa-libgl-devel') }}"
    - sel(linux): "{{ cdt('mesa-dri-drivers') }}"
    - sel(linux): "{{ cdt('libselinux') }}"
    - sel(linux): "{{ cdt('libxdamage') }}"
    - sel(linux): "{{ cdt('libxxf86vm') }}"
    - sel(linux): "{{ cdt('libxfixes') }}"
    - sel(linux): "{{ cdt('libxext') }}"
    - sel(linux): "{{ cdt('libxau') }}"
    - sel(linux): "{{ cdt('libxcb') }}"
  host:
    - python
    - qt-main
    - ros-humble-ament-cmake
    - ros-humble-ament-cmake-pytest
    - ros-humble-pluginlib
    - ros-humble-python-qt-binding
    - ros-humble-rcpputils
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros-humble-tinyxml2-vendor
    - ros2-distro-mutex 0.1 humble
    - sel(build_platform == target_platform): pkg-config
    - sel(build_platform == target_platform): pyqt-builder
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
  run:
    - pyqt-builder
    - python
    - ros-humble-pluginlib
    - ros-humble-qt-gui
    - ros-humble-rcpputils
    - ros-humble-ros-workspace
    - ros-humble-tinyxml2-vendor
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
