package:
  name: ros-humble-rqt-reconfigure
  version: 1.0.8
source:
  git_url: https://github.com/ros2-gbp/rqt_reconfigure-release.git
  git_rev: release/humble/rqt_reconfigure/1.0.8-3
  folder: ros-humble-rqt-reconfigure/src/work

build:
  script:
    sel(win): bld_ament_python.bat
    sel(unix): build_ament_python.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-humble-ament-copyright
    - ros-humble-ament-flake8
    - ros-humble-ament-xmllint
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.1 humble
    - setuptools
  run:
    - python
    - pyyaml
    - ros-humble-ament-index-python
    - ros-humble-python-qt-binding
    - ros-humble-qt-gui-py-common
    - ros-humble-rclpy
    - ros-humble-ros-workspace
    - ros-humble-rqt-console
    - ros-humble-rqt-gui
    - ros-humble-rqt-gui-py
    - ros-humble-rqt-py-common
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
