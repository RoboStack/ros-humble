package:
  name: ros-humble-ignition-math6-vendor
  version: 0.0.2
source:
  git_url: https://github.com/ros2-gbp/ignition_math6_vendor-release.git
  git_rev: release/humble/ignition_math6_vendor/0.0.2-2
  folder: ros-humble-ignition-math6-vendor/src/work

build:
  script:
    sel(win): bld_catkin.bat
    sel(unix): build_catkin.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
    - git
  host:
    - eigen
    - libignition-math6
    - python
    - ros-humble-ament-cmake-copyright
    - ros-humble-ament-cmake-lint-cmake
    - ros-humble-ament-cmake-test
    - ros-humble-ament-cmake-xmllint
    - ros-humble-ignition-cmake2-vendor
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.1 humble
  run:
    - libignition-math6
    - python
    - ros-humble-ignition-cmake2-vendor
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
