package:
  name: ros-humble-hardware-interface
  version: 2.12.0
source:
  git_url: https://github.com/ros2-gbp/ros2_control-release.git
  git_rev: release/humble/hardware_interface/2.12.0-1
  folder: ros-humble-hardware-interface/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-humble-ament-cmake
    - ros-humble-ament-cmake-gmock
    - ros-humble-control-msgs
    - ros-humble-lifecycle-msgs
    - ros-humble-pluginlib
    - ros-humble-rclcpp-lifecycle
    - ros-humble-rcpputils
    - ros-humble-rcutils
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros-humble-ros2-control-test-assets
    - ros-humble-tinyxml2-vendor
    - ros2-distro-mutex 0.1 humble
  run:
    - python
    - ros-humble-control-msgs
    - ros-humble-lifecycle-msgs
    - ros-humble-pluginlib
    - ros-humble-rclcpp-lifecycle
    - ros-humble-rcpputils
    - ros-humble-rcutils
    - ros-humble-ros-workspace
    - ros-humble-tinyxml2-vendor
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
