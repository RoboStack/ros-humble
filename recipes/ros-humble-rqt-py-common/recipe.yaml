package:
  name: ros-humble-rqt-py-common
  version: 1.1.4
source:
  git_url: https://github.com/ros2-gbp/rqt-release.git
  git_rev: release/humble/rqt_py_common/1.1.4-1
  folder: ros-humble-rqt-py-common/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
    - sel(build_platform != target_platform): numpy
  host:
    - numpy
    - python
    - qt-main
    - ros-humble-ament-cmake
    - ros-humble-ament-cmake-pytest
    - ros-humble-python-cmake-module
    - ros-humble-rclpy
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros-humble-rosidl-default-generators
    - ros-humble-rosidl-default-runtime
    - ros2-distro-mutex 0.1 humble
  run:
    - python
    - qt-main
    - ros-humble-python-qt-binding
    - ros-humble-qt-gui
    - ros-humble-rclpy
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
