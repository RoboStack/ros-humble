package:
  name: ros-humble-srdfdom
  version: 2.0.4
source:
  git_url: https://github.com/ros2-gbp/srdfdom-release.git
  git_rev: release/humble/srdfdom/2.0.4-1
  folder: ros-humble-srdfdom/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 5
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - sel(linux64): sysroot_linux-64 2.17
    - ninja
    - setuptools
    - sel(unix): make
    - sel(unix): coreutils
    - sel(osx): tapi
    - sel(build_platform != target_platform): pkg-config
    - cmake
    - cython
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): numpy
  host:
    - console_bridge
    - libboost-devel
    - numpy
    - pip
    - sel(build_platform == target_platform): pkg-config
    - python
    - ros-humble-ament-cmake
    - ros-humble-ament-cmake-gtest
    - ros-humble-ament-cmake-pytest
    - ros-humble-ament-cmake-python
    - ros-humble-ament-lint-auto
    - ros-humble-ament-lint-cmake
    - ros-humble-console-bridge-vendor
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros-humble-tinyxml2-vendor
    - ros-humble-urdf
    - ros-humble-urdfdom-headers
    - ros2-distro-mutex 0.5 humble
  run:
    - console_bridge
    - python
    - ros-humble-console-bridge-vendor
    - ros-humble-ros-workspace
    - ros-humble-tinyxml2-vendor
    - ros-humble-urdf
    - ros-humble-urdfdom-py
    - ros2-distro-mutex 0.5 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
