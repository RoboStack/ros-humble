package:
  name: ros-humble-urdf
  version: 2.6.0
source:
  git_url: https://github.com/ros2-gbp/urdf-release.git
  git_rev: release/humble/urdf/2.6.0-2
  folder: ros-humble-urdf/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-humble-ament-cmake-google-benchmark
    - ros-humble-ament-cmake-ros
    - ros-humble-ament-lint-auto
    - ros-humble-ament-lint-common
    - ros-humble-pluginlib
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros-humble-tinyxml2-vendor
    - ros-humble-urdf-parser-plugin
    - ros-humble-urdfdom
    - ros-humble-urdfdom-headers
    - ros2-distro-mutex 0.1 humble
  run:
    - python
    - ros-humble-pluginlib
    - ros-humble-ros-workspace
    - ros-humble-tinyxml2-vendor
    - ros-humble-urdf-parser-plugin
    - ros-humble-urdfdom
    - ros-humble-urdfdom-headers
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
