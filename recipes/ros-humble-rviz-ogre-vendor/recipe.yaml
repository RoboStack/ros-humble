package:
  name: ros-humble-rviz-ogre-vendor
  version: 11.2.12
source:
  git_url: https://github.com/ros2-gbp/rviz-release.git
  git_rev: release/humble/rviz_ogre_vendor/11.2.12-1
  folder: ros-humble-rviz-ogre-vendor/src/work
  patches:
    - patch/ros-humble-rviz-ogre-vendor.patch

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 7
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - sel(linux64): sysroot_linux-64 2.17
    - ninja
    - setuptools
    - sel(unix): make
    - sel(unix): coreutils
    - sel(osx): tapi
    - sel(build_platform != target_platform): pkg-config
    - cmake
    - cython
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): numpy
    - sel(build_platform != target_platform): git
    - sel(linux): "{{ cdt('mesa-libgl-devel') }}"
    - sel(linux): "{{ cdt('mesa-dri-drivers') }}"
    - sel(linux): "{{ cdt('libselinux') }}"
    - sel(linux): "{{ cdt('libxdamage') }}"
    - sel(linux): "{{ cdt('libxxf86vm') }}"
    - sel(linux): "{{ cdt('libxfixes') }}"
    - sel(linux): "{{ cdt('libxext') }}"
    - sel(linux): "{{ cdt('libxau') }}"
    - sel(linux): "{{ cdt('libxcb') }}"
  host:
    - assimp
    - freeimage
    - freetype
    - numpy
    - pip
    - sel(build_platform == target_platform): pkg-config
    - pkg-config
    - pugixml
    - python
    - ros-humble-ament-cmake
    - ros-humble-ament-cmake-xmllint
    - ros-humble-ament-lint-auto
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.5 humble
    - xorg-libx11
    - zlib
    - zziplib
    - sel(build_platform == target_platform): git
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
  run:
    - assimp
    - freetype
    - python
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.5 humble
    - xorg-libx11
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
