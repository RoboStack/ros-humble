package:
  name: ros-humble-ament-package
  version: 0.14.0
source:
  git_url: https://github.com/ros2-gbp/ament_package-release.git
  git_rev: release/humble/ament_package/0.14.0-4
  folder: ros-humble-ament-package/src/work
  patches:
    - patch/ros-humble-ament-package.patch

build:
  script:
    sel(win): bld_ament_python.bat
    sel(unix): build_ament_python.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - flake8
    - importlib-metadata
    - importlib_resources
    - pytest
    - python
    - ros2-distro-mutex 0.1 humble
    - setuptools
  run:
    - importlib-metadata
    - importlib_resources
    - python
    - ros2-distro-mutex 0.1 humble
    - setuptools
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
