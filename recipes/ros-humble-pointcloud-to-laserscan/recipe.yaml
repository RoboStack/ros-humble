# yaml-language-server: $schema=https://raw.githubusercontent.com/prefix-dev/recipe-format/main/schema.json

package:
  name: ros-humble-pointcloud-to-laserscan
  version: 2.0.1
source:
  git: https://github.com/ros2-gbp/pointcloud_to_laserscan-release.git
  tag: release/humble/pointcloud_to_laserscan/2.0.1-3
  target_directory: ros-humble-pointcloud-to-laserscan/src/work
  patches:
    - patch/ros-humble-pointcloud-to-laserscan.win.patch

build:
  script: ${{ '$RECIPE_DIR/build_ament_cmake.sh' if unix or wasm32 else '%RECIPE_DIR%\\bld_ament_cmake.bat' }}
  number: 15
  post_process:
    - files:
        - '*.pc'
      regex: (?:-L|-I)?"?([^;\s]+/sysroot/)
      replacement: $$(CONDA_BUILD_SYSROOT_S)
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/sysroot)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
    - files:
        - '*.cmake'
      regex: ([^;\s"]+/MacOSX\d*\.?\d*\.sdk)
      replacement: $$ENV{CONDA_BUILD_SYSROOT}
about:
  homepage: http://ros.org/wiki/perception_pcl
  repository: https://github.com/ros-perception/perception_pcl
  license: BSD-3-Clause
  summary: Converts a 3D Point Cloud into a 2D laser scan. This is useful for making devices like the Kinect appear like a laser scanner for 2D-based algorithms (e.g. laser-based SLAM).
extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - ${{ compiler('cxx') }}
    - ${{ compiler('c') }}
    - if: target_platform!='emscripten-wasm32'
      then:
        - ${{ stdlib('c') }}
    - ninja
    - python
    - setuptools
    - git
    - git-lfs
    - if: unix
      then:
        - patch
        - make
        - coreutils
    - if: win
      then:
        - m2-patch
    - if: osx
      then:
        - tapi
    - if: build_platform != target_platform
      then:
        - pkg-config
    - cmake
    - cython
    - if: build_platform != target_platform
      then:
        - python
        - cross-python_${{ target_platform }}
        - numpy
  host:
    - if: build_platform == target_platform
      then:
        - pkg-config
    - numpy
    - pip
    - python
    - ros-humble-ament-cmake
    - ros-humble-ament-cmake-cppcheck
    - ros-humble-ament-cmake-cpplint
    - ros-humble-ament-cmake-flake8
    - ros-humble-ament-cmake-lint-cmake
    - ros-humble-ament-cmake-pep257
    - ros-humble-ament-cmake-uncrustify
    - ros-humble-ament-cmake-xmllint
    - ros-humble-ament-lint-auto
    - ros-humble-laser-geometry
    - ros-humble-message-filters
    - ros-humble-rclcpp
    - ros-humble-rclcpp-components
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros-humble-sensor-msgs
    - ros-humble-tf2
    - ros-humble-tf2-ros
    - ros-humble-tf2-sensor-msgs
    - ros2-distro-mutex 0.8.* humble_*
  run:
    - python
    - ros-humble-laser-geometry
    - ros-humble-launch
    - ros-humble-launch-ros
    - ros-humble-message-filters
    - ros-humble-rclcpp
    - ros-humble-rclcpp-components
    - ros-humble-ros-workspace
    - ros-humble-sensor-msgs
    - ros-humble-tf2
    - ros-humble-tf2-ros
    - ros-humble-tf2-sensor-msgs
    - ros2-distro-mutex 0.8.* humble_*
