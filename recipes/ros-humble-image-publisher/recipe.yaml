package:
  name: ros-humble-image-publisher
  version: 3.0.0
source:
  git_url: https://github.com/ros2-gbp/image_pipeline-release.git
  git_rev: release/humble/image_publisher/3.0.0-1
  folder: ros-humble-image-publisher/src/work
  patches:
    - patch/ros-humble-image-publisher.win.patch

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 1
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
  host:
    - python
    - ros-humble-ament-cmake-auto
    - ros-humble-ament-lint-auto
    - ros-humble-ament-lint-common
    - ros-humble-camera-info-manager
    - ros-humble-cv-bridge
    - ros-humble-image-transport
    - ros-humble-rcl-interfaces
    - ros-humble-rclcpp
    - ros-humble-rclcpp-components
    - ros-humble-ros-environment
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.1 humble
  run:
    - python
    - ros-humble-camera-info-manager
    - ros-humble-cv-bridge
    - ros-humble-image-transport
    - ros-humble-rcl-interfaces
    - ros-humble-rclcpp
    - ros-humble-rclcpp-components
    - ros-humble-ros-workspace
    - ros2-distro-mutex 0.1 humble
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
