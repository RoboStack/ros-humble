tests:
  - script:
    - cmake-package-check rosx_introspection --targets rosx_introspection::rosx_introspection
    requirements:
      run:
        - cmake-package-check
        - ${{ compiler('c') }}
        - ${{ compiler('cxx') }}
